PageDots reports position in a Pager.

```jsx
<PageDots count={3} index={i} />
```

Dots at `space.s8`, the active one widening to `space.s20` in emerald. Over media pass `onMedia` so they switch to on-media ink.
